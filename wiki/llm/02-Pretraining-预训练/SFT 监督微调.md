
![](../_assets/Pretraining-%E9%A2%84%E8%AE%AD%E7%BB%83/SFT%E7%9B%91%E7%9D%A3%E5%BE%AE%E8%B0%83%E7%9A%84%E6%95%B0%E6%8D%AE%E6%B5%81%E7%A8%8B.png)
## 一、 SFT 的核心定位与理论基础

关联阅读：可结合 [大模型推理与交互 (Eval)](../01-Architecture-%E6%A8%A1%E5%9E%8B%E6%9E%B6%E6%9E%84/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%8E%A8%E7%90%86%E4%B8%8E%E4%BA%A4%E4%BA%92%20%28Eval%29.md) 理解；SFT 产出的模型需要通过推理与评测验证。


### 1. SFT 在大模型生命周期中的位置

现代大语言模型（LLM）的训练标准流水线为三阶段范式：

1. **预训练（Pre-training）：** 注入海量世界知识，模型学会“文字接龙”（Next-token prediction）。此时为“基座模型（Base Model）”。
    
2. **监督微调（SFT）：** 注入人类指令与回复规范，模型学会“听从指令并完成任务”。此时为“指令微调模型（Instruct Model）”。
    
3. **人类偏好对齐（RLHF/DPO 等）：** 注入人类价值观，使输出更加安全、有用。

### 2. SFT 的本质目标

打破预训练模型“无目的续写”的惯性，进行**行为克隆（Behavior Cloning）**。通过高质量的 `<指令, 回复>` 数据对，让模型学习：当接收到明确指令时，直接给出确切答案，并学会在适当的时候停止生成。

### 3. 数学原理与损失函数

SFT 依然基于自回归生成机制，但与预训练最大的区别在于：**SFT 通常只对“模型回复（Assistant Response）”部分计算损失（Loss），而忽略“系统提示（System）”和“用户指令（User）”部分。**

其交叉熵损失函数（Cross-Entropy Loss）表示为（其中 X 为输入指令，Y 为目标回复的 Token 序列）：

$$\mathcal{L}_{\text{SFT}} = -\sum_{i=1}^{n} \log P(y_{i} \mid X, y_{<i}; \theta)$$

---

## 二、 SFT 数据工程

在 SFT 阶段，**数据质量远大于数据数量**（Less is More）。如何对数据进行前处理和后处理，是 SFT 炼丹的核心手艺。

### 1. 数据增强 (pre_processing_chat)

- **痛点：** 模型容易对固定的 System Prompt 产生过拟合。如果训练数据中 100% 带有系统提示，模型在实际面对没有系统提示的裸输入时会不知所措。
    
- **工程解法：** 采用**一定概率（如 20%）随机插入**不同风格的 System Prompt。这强制模型学会在有系统指令时遵循设定（如扮演特定角色），在无系统指令时也能作为通用助手正常交流，极大提升了模型的鲁棒性和泛化能力。

### 2. 清洗数据 (post_processing_chat)

- **痛点：** 数据中可能存在瑕疵。例如带有 `<think>` 标签的思维链（CoT）数据，模板渲染后可能产生空的 `<think>\n\n</think>` 占位符。如果原样喂给模型，模型会学到这种“假装思考”的坏习惯。
    
- **工程解法：** 进行正则化清理。以极高概率（如 95%）**剔除无意义的空标签**，确保模型学到的是高质量的输出。同时保留极小概率（如 5%）的边界情况，让模型在实际推理偶尔生成空标签时，具备跳过并继续生成的容错能力，防止陷入死循环。
---

## 三、 SFT 核心代码实现：标签掩码

这是 SFT 代码流中最核心的机制，直接对应了上述“只对回复计算 Loss”的数学原理。

### 1. `create_chat_prompt`：组装标准对话流

在预训练阶段，模型吃的是连篇累牍的纯文本（比如整篇维基百科）。但在 SFT 阶段，我们需要让模型区分“谁在说话”。

- **`apply_chat_template` 的作用：** 大部分开源模型（如 Llama, Qwen, ChatGLM）底层都有一套特定的标记语言。这个方法会自动把你传入的 `[{"role": "user", "content": "..."}]` 字典，渲染成带有 `<|im_start|>`、`<|im_end|>` 等特殊标识符的长字符串。

### 2. `generate_labels`： 滑动窗口与 `-100` 掩码

在 PyTorch 中，交叉熵损失函数的默认忽略索引是 **-100**。为了让模型只学习回答，我们需要构造一个与输入序列等长的 `labels` 序列：

1. **全局初始化：** 先将整句话的 `labels` 初始化为全部是 `-100`，默认不参与梯度更新。
    
2. **寻找锚点：** 通过扫描分词后的 ID，精准定位 `assistant` 回复的起始标记（BOS）和结束标记（EOS）。
    
3. **精准暴露：** 只有在这个 `[BOS, EOS]` 区间内的 `labels`，才会被赋予真实的 Token ID。模型只会根据这部分的误差来更新网络权重。
    
4. **教模型“闭嘴”：** 必须将 EOS（结束符）也包含在参与计算 Loss 的区间内，否则模型推理时会丧失停止生成的能力，导致无休止的“胡言乱语”。

---

## 四、 SFT 数据加载`__getitem__`

一条原始对话数据从 JSON 走向 GPU 显存，通常经历以下标准化组装车间：

1. **提取：** 读取多轮对话的原始数据。
    
2. **增强：** 随机注入 System Persona（人设）。
    
3. **渲染：** 使用特定大模型的 Chat Template 拼接完整字符串。
    
4. **清洗：** 过滤脏数据/空标签（如处理失效的 CoT 块）。
    
5. **分词与对齐（Tokenization & Padding）：** 文本转 ID。超长则截断（Truncation），过短则在右侧使用 `pad_token_id` 补齐长度。
    
6. **打标签（Labeling）：** 生成稀疏的 `-100` 掩码标签。
    
7. **注意力掩码（Attention Mask）：** 生成二进制矩阵，指示模型的 Attention 层忽略为了对齐而补充的 Pad Token。
---

## 五、 主流训练方案选型参考

在完成数据准备后，根据算力资源，SFT 通常采用以下两种参数更新策略：

- **全参数微调（FFT）：** 更新所有权重。模型吸收知识最透彻，但需要庞大的显存集群，且容易引发灾难性遗忘（忘记预训练阶段的通用知识）。
    
- **参数高效微调（PEFT / LoRA）：** 冻结主干网络，注入并训练低秩旁路矩阵（LoRA）。结合量化技术（如 4-bit 的 QLoRA），可以将显存需求降低几个数量级，是目前学术界和工业界最高效、最主流的 SFT 炼丹方案。

## 相关笔记

- **上游**：[大模型推理与交互 (Eval)](../01-Architecture-%E6%A8%A1%E5%9E%8B%E6%9E%B6%E6%9E%84/%E5%A4%A7%E6%A8%A1%E5%9E%8B%E6%8E%A8%E7%90%86%E4%B8%8E%E4%BA%A4%E4%BA%92%20%28Eval%29.md) — SFT 产出的模型需要通过推理与评测验证。
- **前置**：[LLM预训练底层机制](LLM%E9%A2%84%E8%AE%AD%E7%BB%83%E5%BA%95%E5%B1%82%E6%9C%BA%E5%88%B6.md) — 监督微调默认模型已经完成通用预训练。
- **应用**：[LoRA（Low-Rank Adaptation，低秩微调）](LoRA%EF%BC%88Low-Rank%20Adaptation%EF%BC%8C%E4%BD%8E%E7%A7%A9%E5%BE%AE%E8%B0%83%EF%BC%89.md) — SFT 可选择 LoRA 作为参数高效训练方式。
- **对比**：[PretrainDataset和pretrain和utils方法](PretrainDataset%E5%92%8Cpretrain%E5%92%8Cutils%E6%96%B9%E6%B3%95.md) — SFT 数据管线在预训练样本处理上增加对话与掩码逻辑。
- **阶段关系**：[DPO (直接偏好优化)](../03-Post-Training-%E5%90%8E%E8%AE%AD%E7%BB%83/DPO%20%28%E7%9B%B4%E6%8E%A5%E5%81%8F%E5%A5%BD%E4%BC%98%E5%8C%96%29.md) — DPO 通常从经过 SFT 的策略模型开始偏好优化。
- **阶段关系**：[GRPO (组相对策略优化)](../03-Post-Training-%E5%90%8E%E8%AE%AD%E7%BB%83/GRPO%20%28%E7%BB%84%E7%9B%B8%E5%AF%B9%E7%AD%96%E7%95%A5%E4%BC%98%E5%8C%96%29.md) — GRPO 在 SFT 模型基础上进一步优化可验证奖励。
- **项目应用**：MiniMind 项目问答 — SFT 是 MiniMind 完整训练链路中的指令微调阶段。
